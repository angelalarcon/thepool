<%= form_for(@behavior) do |f| %>
  <% if @behavior.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@behavior.errors.count, "error") %> prohibited this behavior from being saved:</h2>

      <ul>
      <% @behavior.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, "Nombre del comportamiento" %>
    <%= f.text_field :name, class: "form-control"%>
  </div>
  <div class="form-group">
    <%= f.label :icon_url, "URL del comportamiento" %>
    <%= f.text_field :icon_url, class: "form-control"  %>
  </div>
  <div class="form-group">
    <%= f.label :icon_code, "Codigo del icono" %>
    <%= f.text_field :icon_code, class: "form-control"  %>
  </div>
  <div class="form-group">
    <%= f.label :icon_type, "Tipo de codigo" %>
    <%= f.text_field :icon_type, class: "form-control"  %>
  </div>

  <div><%= f.submit "Agregar", class: "btn btn-primary btn-block" %></div>
<% end %>
